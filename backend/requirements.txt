# Core service (matches the sandbox/runtime environment)
fastapi==0.128.0
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.2
httpx>=0.27
numpy>=1.26
pdfplumber>=0.11
Pillow>=10.0
python-multipart>=0.0.9

# Optional — neural embeddings (Part B upgrade; falls back to feature hashing)
sentence-transformers>=3.0

# Optional — OCR fallback for /ask-image when no VLM is served
pytesseract>=0.3.10   # requires the tesseract binary (see Dockerfile)
